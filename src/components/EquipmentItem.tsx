import React from 'react';\nimport { View, Text, StyleSheet } from 'react-native';\nimport { EquipmentInstance } from '@/types/game';\nimport { RARITY_COLORS } from '@/data/constants';\n\ninterface EquipmentItemProps {\n  item: EquipmentInstance;\n}\n\n/**\n * Equipment Item Display Component\n */\nexport const EquipmentItem: React.FC<EquipmentItemProps> = ({ item }) => {\n  const rarityColor = RARITY_COLORS[item.equipment.rarity];\n\n  return (\n    <View style={[styles.container, { borderLeftColor: rarityColor }]}>\n      <View style={styles.header}>\n        <Text style={styles.name}>{item.equipment.name}</Text>\n        <Text style={[styles.rarity, { color: rarityColor }]}>\n          {item.equipment.rarity.toUpperCase()}\n        </Text>\n      </View>\n\n      <Text style={styles.description}>{item.equipment.description}</Text>\n\n      <View style={styles.stats}>\n        {item.equipment.baseDamage > 0 && (\n          <Text style={styles.stat}>⚔️ DMG: {item.equipment.baseDamage}</Text>\n        )}\n        {item.equipment.baseDefense > 0 && (\n          <Text style={styles.stat}>🛡️ DEF: {item.equipment.baseDefense}</Text>\n        )}\n      </View>\n\n      <View style={styles.footer}>\n        <Text style={styles.level}>Lv {item.level}</Text>\n        <Text style={styles.quality}>Quality: {item.quality}%</Text>\n      </View>\n    </View>\n  );\n};\n\nconst styles = StyleSheet.create({\n  container: {\n    backgroundColor: '#FFF',\n    borderRadius: 8,\n    padding: 12,\n    marginVertical: 8,\n    borderLeftWidth: 4,\n    shadowColor: '#000',\n    shadowOffset: { width: 0, height: 2 },\n    shadowOpacity: 0.1,\n    shadowRadius: 3,\n    elevation: 3,\n  },\n  header: {\n    flexDirection: 'row',\n    justifyContent: 'space-between',\n    alignItems: 'center',\n    marginBottom: 8,\n  },\n  name: {\n    fontSize: 16,\n    fontWeight: '700',\n    color: '#2C3E50',\n    flex: 1,\n  },\n  rarity: {\n    fontSize: 10,\n    fontWeight: '600',\n  },\n  description: {\n    fontSize: 12,\n    color: '#7F8C8D',\n    marginBottom: 8,\n  },\n  stats: {\n    flexDirection: 'row',\n    marginBottom: 8,\n  },\n  stat: {\n    fontSize: 12,\n    marginRight: 12,\n    color: '#34495E',\n  },\n  footer: {\n    flexDirection: 'row',\n    justifyContent: 'space-between',\n    borderTopWidth: 1,\n    borderTopColor: '#ECF0F1',\n    paddingTop: 8,\n  },\n  level: {\n    fontSize: 12,\n    fontWeight: '600',\n    color: '#2C3E50',\n  },\n  quality: {\n    fontSize: 12,\n    color: '#27AE60',\n  },\n});\n